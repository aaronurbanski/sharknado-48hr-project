/*
import React from "react";

const ZomatoTable = (props) => {
  props.fetchRestaurants();
  return props.obj.restaurants.map((array, index) => {
    return <p key={index}>{array.restaurant.name}</p>;
  });
  return <p></p>;
};

export default ZomatoTable;
*/
